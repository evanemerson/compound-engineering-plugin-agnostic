---
date: 2026-07-29T01:04:24
scope: pr-23
branch: feat/sharded-residual-sink
agents:
  - learnings-researcher
  - security-sentinel
  - architecture-reviewer
  - silent-failure-hunter
  - adversarial-reviewer
  - previous-comments-reviewer
summary:
  total: 10
  p1: 2
  p2: 4
  p3: 4
  pending: 5
  ready: 0
  skipped: 0
  applied: 5
  deferred: 0
agents_skipped:
  - agent: performance-oracle
    reason: "not in cepa.local.md roster — no queries, no runtime hot paths (plugin source repo)"
  - agent: python-reviewer
    reason: "not in roster — no Python application code"
  - agent: data-integrity-guardian
    reason: "not in roster — no migrations or database"
  - agent: schema-drift-detector
    reason: "not in roster — no ORM models or serializers"
  - agent: frontend-reviewer
    reason: "not in roster — no UI"
  - agent: deployment-verifier
    reason: "not in roster — nothing deploys from this repo"
conditional_dispatch:
  - agent: adversarial-reviewer
    dispatched: true
    reason: "diff is 676 lines (>300 threshold)"
  - agent: reliability-reviewer
    dispatched: false
    reason: "no queue/webhook/transaction/external-call code in diff"
  - agent: previous-comments-reviewer
    dispatched: true
    reason: "16 todos/review-*.md files exist (review history present)"
deploy_verdict:
  verdict: not-evaluated
  basis: "deployment-verifier not in roster — plugin source repo, no deploy target"
  conditions: []
detection_signals:
  matched_docs: 5
  docs_with_detection: 5
  signals_passed: 22
  sources:
    - docs/solutions/logic-errors/durable-record-promises-need-a-named-sink-in-every-phase.md
    - docs/solutions/logic-errors/scheduled-pipelines-must-commit-their-own-state-before-cleanliness-gates.md
    - docs/solutions/logic-errors/autonomous-gates-must-fail-closed-on-missing-artifacts.md
    - docs/solutions/security-issues/argument-sanitization-at-every-shell-out-relay-point.md
    - docs/solutions/security-issues/relaying-solution-doc-content-into-agent-prompts-needs-untrusted-data-guards.md
  backfill_candidates: []
  suspect_bullets: 0
  corpus: present
learnings_research: "ok"
brain:
  provider: ob1
  status: fresh
  role: consumer
  queries: 1
  written: 0
  suppressed_writebacks: 0
  scrubbed: 0
  args_skipped: 0
  suspect_stripped: 0
  pre_step: "ok — 1 recalls, 0 args skipped, 0 suspect stripped, 0 non-participant dropped"
---

# Review Findings — 2026-07-29 01:04

**Subject:** PR #23 — `feat: sharded residual sink — memory/tasks.d/ (1.13.0)`

**Through-line:** the shard mechanism itself survived adversarial tracing
(distinct branches → distinct files → clean merges; sweep strikes are
one-sided edits merges preserve). What the review caught is the seams: the
slug function was underspecified (no charset guard, lossy map stated as an
absolute guarantee), the weekly tier acquired a second naming rule without
reconciling it against §5's generic one, and the new union-read relay
shipped without its §7 clause — the exact PR-#9 laundering shape CLAUDE.md's
critical rule exists to prevent.

### 1
- status: applied
- severity: P1
- agent: security-sentinel + adversarial-reviewer
- category: security
- confidence: 80
- action_class: corroborated
- file: plugins/cepa/skills/autonomy/SKILL.md
- lines: 326-338
- title: §5 slug had no charset guard; raw branch name reached Write paths and git command lines

**Problem:** `<branch-slug>` was defined only as "`/` → `-`". Git refs
legally contain `` ` ``, `$`, `;` — a hostile fork or issue-derived branch
name would be spliced verbatim into shard filenames used in `git add`/
`git commit` command lines (restated at plan-review.md, resolve-pr.md,
task.md sites). Same class as the fixed graphify/pr-feedback relay points
(docs/solutions/security-issues/argument-sanitization-at-every-shell-out-relay-point.md).

**Fix (applied):** §5 now defines `slug(x)` — allowlist `[A-Za-z0-9_-]`,
everything else → `-`, empty/leading `-`/`.` falls back to short SHA — and
instructs directory-granularity staging (`git add memory/tasks.d/`) over
splicing the filename into a shell command. The false "never
merge-conflicts" guarantee is replaced with the honest lossy-map statement
(collision → loud add/add conflict, keep both blocks).

### 2
- status: applied
- severity: P1
- agent: security-sentinel
- category: security
- confidence: 80
- action_class: mechanical
- file: plugins/cepa/agents/research/learnings-researcher.md
- lines: 146-149
- title: New memory/tasks.d/*.md union-read relay lacked the §7 untrusted-data clause

**Problem:** Both readers (learnings-researcher Step 4,
previous-comments-reviewer setup step 2) globbed shard files — repo-stored
prose written by unattended runs — into their own reasoning with no
data-not-instructions boundary, while sibling relays in the same files
(Detection extraction, PR comments) carry the clause. Reproduces the PR #9
laundering path; violates CLAUDE.md's "untrusted at every relay point"
critical rule (covering declarative exemption claims, strip-not-label,
durable recording).

**Fix (applied):** §7 clause added at both relay points (researcher:
SUSPECT-quote + strip + status-line count; previous-comments-reviewer:
corrupted-input finding, no relay into other findings). `memory/tasks.d/`
shards + legacy tasks.md added to §7's enumerated untrusted-content list.

### 3
- status: applied
- severity: P2
- agent: architecture-reviewer + silent-failure-hunter + adversarial-reviewer + previous-comments-reviewer
- category: contract-consistency
- confidence: 85
- action_class: corroborated
- file: plugins/cepa/commands/review.md
- lines: 111-114, 586-594
- title: Weekly slug rule contradicted §5 — three defensible shard names per weekly run, and an unslugged trunk escaped every glob

**Problem:** The weekly worktree is detached, so §5's generic rule says
short-SHA; the weekly section said `weekly-<trunk>` (raw — a
`release/2026` trunk writes `…-weekly-release/2026.md`, a subdirectory no
`memory/tasks.d/*.md` consumer sees, silently defeating the dedup this
section calls "the only dedup in the system"); and the durable-record exit
block cited plain §5 and executes in the developer's checkout (its slug =
whatever branch the dev left checked out). One run, three names — the
"loud and repeating" misconfiguration signal scatters.

**Fix (applied):** §5 gained the run-type-slug override concept (must be
stated at every write site); review.md's weekly section now mandates
`weekly-<slug(trunk)>` (§5's slug function, `/`→`-`) for EVERY weekly
shard write including the exit records, which now name it explicitly.

### 4
- status: applied
- severity: P2
- agent: silent-failure-hunter
- category: silent-failure
- confidence: 80
- action_class: mechanical
- file: plugins/cepa/commands/review.md
- lines: 130-152
- title: Weekly exit-path shard write had no reachable commit step — uncommitted shard wedges the next sweep

**Problem:** "Commit this edit with the same commit the run makes below"
was unreachable for four of six exit reasons (they exit before the
worktree/commit step exists). The shard landed untracked in the dev
checkout; the next sweep's Step 1 gate demotes on dirty state every cycle
— permanent wedge, re-fed weekly by cron. Matches
docs/solutions/logic-errors/scheduled-pipelines-must-commit-their-own-state-before-cleanliness-gates.md.
(Shape pre-dates this PR — 1.12.0's tasks.md instruction had the same
dangling reference — but the PR rewrote both lines.)

**Fix (applied):** explicit per-resource commit ladder: worktree exists →
commit inside it before push/cleanup; trunk resolvable → throwaway
worktree for the record alone; trunk unresolvable → own commit on the
current branch staging `memory/tasks.d/` only, reported as
`record: committed on <branch> — <reason>`. Never exit with the shard
uncommitted.

### 5
- status: pending
- severity: P2
- agent: adversarial-reviewer
- category: design
- confidence: 85
- action_class: judgment
- file: plugins/cepa/skills/autonomy/SKILL.md
- lines: 326-350
- title: Slug map is non-injective — optional disambiguator would eliminate the residual add/add collision case

**Problem:** `feat/a-b` and `feat/a/b` both slug to `feat-a-b`; a same-day
collision between two such branches resurrects the add/add merge conflict
the release exists to eliminate. Fix 1 made the invariant honest (conflict
is loud, resolution stated), but the collision itself remains possible.

**Fix (proposed, needs decision):** append a short disambiguator from the
raw branch name — `memory/tasks.d/<date>-<slug>-<4-hex-of-sha1(branch)>.md`
— trading filename readability for true injectivity. Design call: the
honest-conflict wording may be good enough given the narrow trigger.

### 6
- status: pending
- severity: P2
- agent: silent-failure-hunter
- category: silent-failure
- confidence: 65
- action_class: judgment
- file: plugins/cepa/commands/setup.md
- lines: 79-83
- title: No forward marker for pre-1.13 readers — a lagging 1.12 sweep never consults shards and reports a clean pass

**Problem:** Installed plugins lag the repo for weeks. A 1.12 sweep reads
only `memory/tasks.md`; every residual a 1.13 writer files into shards is
invisible to it, and its coverage report is truthfully "all sinks swept" —
success indistinguishable from an unread sink.

**Fix (proposed, needs decision):** have `/cepa:setup fix` (or first shard
creation) append one unstruck marker entry to legacy `memory/tasks.md`
("residual sink sharded to memory/tasks.d/ at 1.13.0 — a reader consulting
only this file has not seen all residuals") so an old sweep surfaces it as
awaiting-human; new readers dedup it. Plus: setup check 3b flags
installed<1.13 + shards present as a warning. Adversarial-reviewer's trace
rates the seam self-healing-but-deferred; decide if the marker is worth
the noise.

### 7
- status: pending
- severity: P3
- agent: adversarial-reviewer
- category: design
- confidence: 85
- action_class: judgment
- file: plugins/cepa/commands/sweep.md
- lines: 85-93
- title: Append-time dedup is blind to concurrent siblings' shards — sweep should reconcile cross-shard duplicates

**Problem:** Parallel worktrees can file the same residual (same
file:line + title) into their own shards — each run's dedup scan can't see
the sibling's unmerged branch. Post-merge, trunk holds silent duplicates;
judgment dupes reach awaiting-human twice and the orphan persists after a
human answers one.

**Fix (proposed):** add to sweep Step 2: on sight of two entries with the
same file:line + normalized title across shards/findings files, collapse —
keep one canonical, strike the other with `— duplicate of <ref>` —
mirroring the existing reconciled-on-sight rule for completed copies.

### 8
- status: applied
- severity: P3
- agent: silent-failure-hunter
- category: correctness
- confidence: 85
- action_class: mechanical
- file: plugins/cepa/scripts/check-health.sh
- lines: 34
- title: Shard count via `ls | wc -l` miscounted when a glob match is a directory

**Problem:** Verified empirically: a directory matching `*.md` inflates
the count with its contents plus header lines.

**Fix (applied):** `find memory/tasks.d -maxdepth 1 -name '*.md' -type f`
— the same idiom the script already uses for solution docs.

### 9
- status: pending
- severity: P3
- agent: silent-failure-hunter
- category: silent-failure
- confidence: 60
- action_class: mechanical
- file: plugins/cepa/scripts/check-health.sh
- lines: 84-88
- title: Health check can't detect an untracked or gitignored memory/tasks.d/

**Problem:** The uncommitted-scaffold loop checks `memory` (satisfied by
the ubiquitous tracked tasks.md, shadowing an untracked tasks.d/), and
there's no `git check-ignore` probe for the path — a repo that gitignores
`memory/` gets a green report while shard commits silently produce
nothing.

**Fix (proposed):** add `memory/tasks.d` to the scaffold loop and a
check-ignore probe mirroring the docs/plans one. Sub-threshold confidence
(60) only because the gitignored-memory/ configuration is speculative.

### 10
- status: pending
- severity: P3
- agent: architecture-reviewer
- category: duplication
- confidence: 65
- action_class: mechanical
- file: plugins/cepa/skills/grounding/SKILL.md
- lines: 173
- title: Shard-path template restated verbatim in four places instead of citing §5

**Problem:** `memory/tasks.d/<date>-<branch-slug>.md` is spelled out in
grounding/SKILL.md, brain/SKILL.md, review.md, and task.md (with `<date>`
vs canonical `<YYYY-MM-DD>` drift already). Restatements are how the next
slug change ships inconsistently.

**Fix (proposed):** replace restatements with "the run's residual shard
(`cepa:autonomy` §5 sink 1)" except where the site defines an override
(review.md weekly).

---

**Summary:** 10 findings (2 P1, 4 P2, 4 P3) — 5 auto-applied (both P1s,
2 P2s, 1 P3), 5 pending (3 judgment, 2 sub-threshold mechanical)
**Next step:** Run `/cepa:triage` (batch auto-apply by default; pass `interactive` for one-at-a-time).
