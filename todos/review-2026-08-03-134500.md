---
date: 2026-08-03T13:45:00
scope: pr:36
branch: fix/hang-path-executable-guard
agents:
  - security-sentinel
  - architecture-reviewer
  - silent-failure-hunter
  - reliability-reviewer
  - previous-comments-reviewer
  - adversarial-reviewer
agents_skipped:
  - agent: python-reviewer
    rule: "not rostered in cepa.local.md — no Python app"
  - agent: performance-oracle
    rule: "not rostered — no queries"
  - agent: data-integrity-guardian
    rule: "not rostered — no migrations"
  - agent: schema-drift-detector
    rule: "not rostered — no ORM"
  - agent: frontend-reviewer
    rule: "not rostered — no UI"
  - agent: deployment-verifier
    rule: "not rostered — nothing deploys from here"
  - agent: pr-test-analyzer
    rule: "not rostered — no test suite"
  - agent: type-design-analyzer
    rule: "not rostered — no types"
conditional_dispatch:
  - agent: adversarial-reviewer
    dispatched: true
    signal: "~870-line diff modifying a gating script whose documented dominant failure mode is the silent pass"
  - agent: reliability-reviewer
    dispatched: true
    signal: "diff changes process-lifecycle and timeout semantics in a scheduled CI harness"
  - agent: previous-comments-reviewer
    dispatched: true
    signal: "diff edits todos/ and memory/tasks.d/ sinks directly; closes a deferred finding"
deploy_verdict: not-evaluated
deploy_basis: "deployment-verifier is not rostered — nothing deploys from this repo"
detection_signals:
  count: 32
  source_docs: 6
  backfill_candidates: 0
  suspect_bullets: 0
learnings_research: "reused from the /cepa:task Phase 2.1 briefing — same topic, same files, same session"
brain:
  pre_step: "ok — health ok, 5 participants resolved"
  health: ok
  participants: 5
summary:
  total: 31
  p1: 4
  p2: 5
  p3: 22
  applied: 22
  deferred: 9
---

# Review — PR #36 (fix/hang-path-executable-guard, 1.18.3)

Six agents. Four P1s, and **three of them were the artifact failing to be the
thing it claims to be** — assertions that could not fail, a guard with no
coverage, an anchor that asserted spelling rather than routing. Every one was
demonstrated by mutation, not argued.

This is the second consecutive round on this branch where review caught the
control passing without the construct it protects. The plan review caught two
such defects before build; these are the ones that survived into code.

## Deploy Verdict

not-evaluated — nothing deploys from this repo.

### 1
status: applied
severity: P1
agent: silent-failure-hunter
category: silent-failure
confidence: 100
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 546-554, 598-609
title: The hang fixture never emitted a trailer, so the truncation and HARNESS-ERROR assertions passed by construction

**Problem:** `st_hang_fixture` had no trailer-emitting statement at all. So "the
kill truncates the transcript before its trailer" and "classifies
HARNESS-ERROR" were asserting that a fixture *incapable* of producing a trailer
did not produce one. Truncation was never demonstrated. Measured: with the
`$( )` pipe restored **and** with the bound widened so it stops firing, both
assertions still PASSED — they contributed nothing in either direction. An
empty transcript already classifies HARNESS-ERROR (asserted at `:401`), so the
classification half was vacuous too. This is plan-review finding #2 half-landing:
the positive-presence half shipped, the truncation half did not.

**Fix:** the fixture emits `-- 2/2 controls passed --` *after* the hang, so a
capture that fails to bound produces a complete, classifiable transcript.
Verified: a fixture that finishes before the bound now fails both assertions
(28/30), reporting `got SURVIVED` — the false mutant result the branch exists
to prevent, which the suite previously could not produce.

applied_in: this branch (same commit as this file)

### 2
status: applied
severity: P1
agent: adversarial-reviewer
category: silent-failure
confidence: 95
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 336-340
title: capture_controls has two guards and only one was under test — deleting the rm -f guard stayed green

**Problem:** The extraction was justified as putting the real path under test,
but every selftest call targeted a freshly `mktemp -d`'d path where `out_file`
cannot pre-exist, so `rm -f` was a no-op on every run and invisible to every
assertion. Measured: deleting the guard entirely → **27/27 green**, and the
false CAUGHT it exists to prevent reproduces (stale RED transcript + a failed
open → mutant N classified from mutant N-1's transcript → `CLASS=CAUGHT` for a
mutant that actually SURVIVED). A softer edit — `rm -f … || :` — was also green.
The `code-simplifier` agent is rostered weekly and would plausibly read the
`>`-truncates-on-open comment and delete it as redundant.

**Fix:** a case that makes the clear itself fail (transcript pre-seeded with a
complete RED transcript in a `chmod 555` directory) and asserts exit 2, run in a
subshell because the guard's failure path is `exit 2`. Skipped as root, where
directory permissions do not deny unlink. Verified: guard deleted → 29/30.

applied_in: this branch (same commit as this file)

### 3
status: applied
severity: P1
agent: adversarial-reviewer
category: silent-failure
confidence: 95
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 665-671
title: The exactly-once anchor counted text, so a commented-out call site satisfied it

**Problem:** The anchor counted a literal in `read_file "$0"` — comments
included. Measured: commenting out the production call site and inlining a
`$( )` capture beside it left the literal intact inside a `#` comment, the
assertion printed **PASS**, and `--selftest` was **27/27, exit 0** while
production had residual finding #12 reintroduced verbatim and `capture_controls`
was dead code. The assertion's own message asserted routing and observed only
spelling. Separately (adversarial F4, conf 90) an innocent line-wrap — in the
house style this block's *own* call uses — dropped the count to 0 and reddened
the weekly job for a whitespace change, costing a week of sweep coverage.

**Fix:** count against a normalized form — drop comment-only lines, join
backslash continuations, collapse space runs. Verified both directions:
commented-out + inlined → 29/30 (red), innocent line-wrap → 30/30 (green).

applied_in: this branch (same commit as this file)

### 4
status: applied
severity: P1
agent: security-sentinel, reliability-reviewer
category: silent-failure
confidence: 100
action_class: corroborated
file: scripts/run-mutation-sweep.sh
lines: 523-529, 570
title: An interrupted selftest cleaned up but did not stop, then reported fabricated control regressions

**Problem:** `trap st_hang_cleanup EXIT INT TERM` installed a handler that
returns normally, and a bash trap that returns does not terminate the script.
Both agents reproduced it independently: SIGINT mid-run deleted the fixture, then
execution *resumed* and ran the remaining ~20s of assertions against a directory
that no longer existed — reporting 21/27 (security) / 24/27 (reliability) and
exit 1, naming the fixture as the culprit. On the CI gate that is
indistinguishable from a genuine classifier regression. It is the "stopped →
regressed" collapse this script exists to refuse, and GitHub Actions sends SIGINT
on cancellation.

**Fix:** EXIT stays idempotent; INT/TERM clean up, restore the default handler,
and re-raise so the script actually terminates with the conventional status.

applied_in: this branch (same commit as this file)

### 5
status: applied
severity: P2
agent: silent-failure-hunter
category: silent-failure
confidence: 95
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 598-599
title: A negative assertion decided by grep read a grep ERROR as "pattern absent" → PASS

**Problem:** The trailer-absence assertion inverted grep's status, so absence ⇒
PASS. grep exits **2** on error (broken ERE, unreadable file), which is equally
non-zero and landed in the PASS branch, with `2>/dev/null` deleting the evidence.
Measured: broken ERE → reports PASS; missing file → reports PASS. The ERE is a
re-typed copy of the classifier's own pattern at `:279` and the two can drift.

**Fix:** branch on the actual status — 0 is a real FAIL, 1 is the asserted
absence, anything else is a FAIL naming that absence was never established.
Stderr no longer discarded.

applied_in: this branch (same commit as this file)

### 6
status: applied
severity: P2
agent: adversarial-reviewer
category: silent-failure
confidence: 90
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 324-346
title: The production bound was entirely unpinned — 900 → 0 disables timeout and stayed green

**Problem:** The anchor's needle stopped before the bound arguments, and nothing
read them. GNU `timeout` treats a duration of 0 as *no timeout at all*, so the
standard "stop bounding this while I investigate" edit removed the bound
completely while `--selftest` stayed **27/27, exit 0** (every hang assertion runs
at its own hardcoded bound). Verified on coreutils 9.4: `timeout -k 30 0 sleep 4`
returns after 4.0s.

**Fix:** validate inside `capture_controls` rather than pinning textually — a
non-numeric or zero bound/grace is fatal. Pinning `30 900` into the needle was
rejected because it enlarges the false-red surface of finding 3. Verified: the
production path with `30 0` now dies at the first mutant with exit 2 and a named
reason. Stated limit: `--selftest` never executes the production call site, so
only the runtime refusal protects it.

applied_in: this branch (same commit as this file)

### 7
status: applied
severity: P2
agent: architecture-reviewer, previous-comments-reviewer
category: record-accuracy
confidence: 100
action_class: corroborated
file: todos/review-2026-08-02-231333.md
lines: 147-158
title: The new-case count was off by one in every durable record

**Problem:** Records and the commit message said "ten cases … plus an anchor"
(= 11); ten were added. Both agents counted from source and confirmed by running
both revisions (main 17, HEAD 27). The records' own arithmetic falsified them:
17 + 11 = 28, but every record reported 27/27. This is CLAUDE.md's named
recurring class (three consecutive PRs), landed in the artifacts that outlive the
branch — and it had already propagated into the review dispatches.

**Fix:** restated as **13 new assertions (17 → 30)** after this round's fixes,
counted from `grep -c '^ *st_assert '` and the live trailer, with the
composition spelled out rather than a bare total.

applied_in: this branch (same commit as this file)

### 8
status: applied
severity: P2
agent: previous-comments-reviewer
category: record-accuracy
confidence: 100
action_class: mechanical
file: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md
lines: 30-31
title: The residual's only pointer to the uncovered exit 2 was born stale by 224 lines

**Problem:** The shard cited `scripts/run-mutation-sweep.sh:675-681`, true of
`main` but not of the commit that wrote it — the same commit inserted 248 lines
above the target. `:675-681` in the shipped file is the selftest's tail, a
different construct in a different code path. A residual exists so the next
person can find the statement; this one had never been correct.

**Fix:** corrected to `:1057-1063`, re-derived from the shipped file after this
round's additional insertions.

applied_in: this branch (same commit as this file)

### 9
status: applied
severity: P2
agent: reliability-reviewer
category: reliability
confidence: 85
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 511-515, 574-575
title: ST_NAP was documented as the only knob while the file arm's bound was hardcoded

**Problem:** The comment claimed "a threshold moves only by moving `ST_NAP`",
but the file arm's grace and bound were passed positionally as `1 3` and did not
scale. The unstated invariant was `bound + grace < ST_NAP/2`. Measured:
`ST_NAP=10` → FAIL, `ST_NAP=8` → FAIL. Lowering `ST_NAP` is the predictable edit,
since 20 of the block's ~24 seconds live there.

**Fix:** `ST_BOUND`/`ST_GRACE` named and used at both call sites, plus a guard
that refuses a configuration with too little margin. First attempt used
`>= ST_NAP/2`, which still admitted the flaky `ST_NAP=10` case; tightened to
require `ST_NAP >= 4*(bound+grace)`. Verified: `ST_NAP=10` now exits 2 with a
message naming the constant rather than reporting a guard regression.

applied_in: this branch (same commit as this file)

### 10
status: applied
severity: P3
agent: architecture-reviewer
category: convention
confidence: 90
action_class: mechanical
file: todos/review-2026-08-02-231333.md
lines: 147
title: The resolved: line omitted the date the canonical spec requires

**Problem:** `cepa:file-todos` defines `resolved` as "date + branch/PR"; all
seven prior instances in this repo lead with the date. The new line led with the
branch. CLAUDE.md: never invent format variants.

**Fix:** `resolved: 2026-08-03 — fix/hang-path-executable-guard (1.18.3) — …`.

applied_in: this branch (same commit as this file)

### 11
status: applied
severity: P3
agent: previous-comments-reviewer
category: record-accuracy
confidence: 95
action_class: mechanical
file: memory/tasks.d/2026-08-01-feat-mutation-sweep-harness.md
lines: 231
title: An open residual still asserted --selftest is "17 assertions"

**Problem:** A present-tense claim made false by this branch. Plan review ran
exactly this audit for the zero-mutant-coverage claim and found its
restatements, but nobody ran it for the selftest count — even though another
finding in the same review was about a wrong selftest count.

**Fix:** `(17 assertions …)` → `(30 assertions …)`. `:299` deliberately NOT
touched — it is 1.18.2's frozen verification record and was true when written;
editing it would fabricate a measurement.

applied_in: this branch (same commit as this file)

### 12
status: applied
severity: P3
agent: security-sentinel
category: correctness
confidence: 72
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 546-554, 523-529, 556-567
title: orphan.pid was a reused path never cleared between plants, feeding kill -9 unvalidated

**Problem:** The reuse discipline the diff states at `capture_controls` was not
applied to `orphan.pid` one function away: the fixture is planted twice and the
second plant left the first arm's PID in place if its write failed (`;` not
`&&`), so a reaper could signal a recycled PID. Neither reaper validated the
value — a leading `-` turns `kill -9 "$p"` into a process-**group** kill, and
`kill -9 -1` signals everything the user owns.

**Fix:** `rm -f "$1/orphan.pid"` at the top of the fixture builder, and a
numeric-only `st_pid_ok` guard consulted by both `st_reap` and the cleanup trap.

applied_in: this branch (same commit as this file)

### 13
status: applied
severity: P3
agent: silent-failure-hunter, previous-comments-reviewer
category: silent-failure
confidence: 95
action_class: corroborated
file: scripts/run-mutation-sweep.sh
lines: 665
title: read_file "$0" || st_self='' reported an unreadable script as a missing call site

**Problem:** The fallback turned a read failure into an empty haystack, count 0,
and a message telling the operator to restore a call site that is present and
correct. Verbatim the inversion `read_file` itself was rewritten to stop making,
that the existing selftest case at `:466-473` guards, and that plan-review
finding #3 cited as the reason to drop `pgrep`. The guard was reasoned about,
applied there, and re-opened at a new site in the same block.

**Fix:** fail with the right cause — FATAL naming an environment failure, exit 2.

applied_in: this branch (same commit as this file)

### 14
status: applied
severity: P3
agent: silent-failure-hunter, security-sentinel
category: silent-failure
confidence: 90
action_class: corroborated
file: scripts/run-mutation-sweep.sh
lines: 342-365
title: A failed date yielded CAP_ELAPSED of 0 or a large negative — both satisfy "returned fast"

**Problem:** Reading `date` into variables first defeats the `[ "" -gt N ]`
variant but not this one: `$((t1 - t0))` treats an empty operand as **0**, and 0
satisfies every "returned fast enough" comparison. Measured: both calls failing →
`CAP_ELAPSED=0` → PASS; only `t1` failing → large negative → PASS. The one place
the file claimed immunity to a class it still carried.

**Fix:** guard the operands and yield `-1`, which fails those comparisons.

applied_in: this branch (same commit as this file)

### 15
status: applied
severity: P3
agent: silent-failure-hunter
category: silent-failure
confidence: 85
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 650, 523-529
title: st_reap's status was discarded at the arm-2 call site; cleanup leaked silently

**Problem:** `st_reap … || :` discarded the only status it returns, so arm 2's
orphan surviving SIGKILL produced a fully green run with no message and no
counter. Separately `st_hang_cleanup` ignored `rm -rf`'s status where the
production `cleanup()` in the same file prints `WARN: work tree leaked at %s`.

**Fix:** assert arm 2's reap like arm 1's, and match the existing
leak-reporting idiom.

applied_in: this branch (same commit as this file)

### 16
status: applied
severity: P3
agent: reliability-reviewer
category: reliability
confidence: 80
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 564
title: sleep 0.1 collapses the reap poll to a busy spin on any non-GNU sleep

**Problem:** Fractional `sleep` is a GNU extension. On busybox without
`FANCY_SLEEP` it errors and returns instantly, so all 40 iterations run in
microseconds: the stated "4s poll" — added specifically to stop an intermittent
FAIL — becomes ~0s, and the assertion fails claiming the orphan "survived
SIGKILL and a 4s poll", false in two ways. Verified with an integer-only `sleep`
stub on PATH.

**Fix:** `sleep 0.1 2>/dev/null || sleep 1`.

applied_in: this branch (same commit as this file)

### 17
status: applied
severity: P3
agent: security-sentinel
category: security
confidence: 55
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 547-553
title: Unquoted-heredoc interpolation of the temp path into a single-quoted bash -c string

**Problem:** The fixture is the one place this file *generates* a script, and
`$ST_HANG_DIR` is interpolated inside a single-quoted `bash -c` argument. A quote
in `TMPDIR` would break out of that quoting. Defense in depth — unreachable in
any sanctioned flow.

**Fix:** refuse a fixture path containing shell metacharacters, immediately
after `mktemp -d`.

applied_in: this branch (same commit as this file)

### 18
status: applied
severity: P3
agent: reliability-reviewer
category: silent-failure
confidence: 60
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 633
title: The pipe arm discarded the reader's stderr, so every failure named one specific cause

**Problem:** `2>/dev/null` collapsed a deleted fixture, a `cd` failure, a missing
`timeout` and an OOM kill into the same `elapsed=-1`, rendered as "the fixture's
own bound failed" — correct for one cause and wrong for the rest. The workflow
file already corrected this same class in its issue-body grep.

**Fix:** stderr no longer discarded on the pipe arm.

applied_in: this branch (same commit as this file)

### 19
status: applied
severity: P3
agent: architecture-reviewer, previous-comments-reviewer
category: record-accuracy
confidence: 80
action_class: corroborated
file: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md
lines: 5-6
title: Both shard headers miscounted the bullets beneath them

**Problem:** The new shard said "1 deferred" over three `- [ ]` bullets (two are
consequences of findings that are themselves `status: applied`); the 08-02 shard
still said "2 deferred below" after one was resolved.

**Fix:** both headers state the count and its composition.

applied_in: this branch (same commit as this file)

### 20
status: applied
severity: P3
agent: previous-comments-reviewer
category: process
confidence: 80
action_class: judgment
file: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md
lines: n/a
title: Plan-review finding #3 was marked applied while only its PID half shipped

**Problem:** The finding specified recording PID **and** PGID and asserting both;
only the PID half shipped. The substituted reasoning (liveness after the capture
returned implies the group-kill missed it) is sound and does discriminate — but
it is an undisclosed narrowing of a corroborated P1 recorded as `applied`.

**Fix:** disclosed in the shard's "Deliberate divergences from an applied
finding" section, with the reason and the residual risk, rather than silently.

applied_in: this branch (same commit as this file)

### 21
status: applied
severity: P3
agent: previous-comments-reviewer, adversarial-reviewer
category: documentation
confidence: 55
action_class: mechanical
file: .github/workflows/mutation-sweep.yml
lines: 81-83
title: The selftest step still called itself "cheap … fails in seconds" after a 325x slowdown

**Problem:** Measured `main` 0.079s → HEAD ~25s. The branch argued elsewhere that
24s makes "finish in seconds" measurably false — and applied the stricter reading
to a file it did not touch while leaving the identical claim on the step it
actually slowed.

**Fix:** amended to state ~25s and why the deliberate waits are what make the
cases evidence.

applied_in: this branch (same commit as this file)

### 22
status: deferred
severity: P3
agent: architecture-reviewer
category: silent-failure
confidence: 85
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 655-671
title: The anchor detects replacement of the call site, never addition beside it

**Problem:** `== 1` cannot express "and nowhere else". Adding a second capture
beside the surviving call site — a plausible retry-on-empty-transcript edit —
keeps the count at 1 and stays green. Finding 3's normalization closes the
comment-out and line-wrap directions but not this one.

**Fix:** pair it with a zero-count assertion on the raw `> "$WORK/controls.out"`
shape, which occurs 0 times today, so the assertion is true on arrival and goes
red on the addition case.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 23
status: deferred
severity: P3
agent: reliability-reviewer
category: reliability
confidence: 40
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 885-886
title: The redirect bounds the driver's wait, not the transcript's writers

**Problem:** An escaped descendant inherits the redirect's fd and keeps it open
after the capture returns — the selftest asserts exactly such a writer is alive
three lines before classifying that file. Not reachable today (no production
descendant has both required properties) and the next mutant is safe because
`rm -f` unlinks the inode.

**Fix:** no cheap total fix — an unlink does not revoke an open fd. Either state
the residual in the `capture_controls` header or add a case asserting a
post-return write cannot flip `CLASS` away from HARNESS-ERROR.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 24
status: deferred
severity: P3
agent: previous-comments-reviewer
category: process
confidence: 75
action_class: judgment
file: docs/solutions/logic-errors/
lines: n/a
title: "Environment failure reported as a code finding" is now four instances in one file with no solution doc

**Problem:** `read_file` returning success on an unreadable path; `scannable_in`
post-assertions reading a `find` failure as a successful plant; `pgrep` failure
reading as "plant no-oped"; and finding 13 above. Each was fixed at the site it
was reported at while the construct stayed live — the exact shape CLAUDE.md
already documents three times for other classes. The nearest existing doc is
about layering, not failure-mode polarity.

**Fix:** run `/cepa:compound` for a `docs/solutions/logic-errors/` entry with a
Detection section keyed on `|| <var>=''`, `2>/dev/null ||`, and bare
`$( … || printf '')` in assertion contexts.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 25
status: deferred
severity: P3
agent: architecture-reviewer
category: convention
confidence: 85
action_class: judgment
file: CLAUDE.md
lines: 109-110
title: The version-scope rule does not cover scripts/, and one cited precedent does not support a scripts-only bump

**Problem:** The rule names only plugin content as bumping. This branch changes
`scripts/`, `todos/` and `memory/` and bumps to 1.18.3. Verified: 1.16.2
(`5dab52c`) and 1.18.2 (`929354f`) are genuine scripts-only bumps and support it,
but **1.16.1 (`a9db65b`) also changed `plugins/cepa/skills/autonomy/SKILL.md`**
and is not a valid precedent for one. Third precedent-derived bump in a repo
whose CLAUDE.md is the contract.

**Fix:** amend the rule to name `scripts/` on the bumping side, citing 1.16.2 and
1.18.2 only. Separate deliverable — belongs on its own branch.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 26
status: deferred
severity: P3
agent: security-sentinel
category: reliability
confidence: 50
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 569, 736-747
title: The selftest's temp-dir creation sits outside the sweep's own TMPDIR-inside-repo guard

**Problem:** The containment check that FATALs when the work dir resolves inside
the repo runs after `--selftest` has already exited, so the fixture dir gets no
equivalent check. Mitigated: the cleanup trap removes the dir on EXIT/INT/TERM
(verified — `git status --porcelain` byte-identical before and after), so only
SIGKILL leaks, and CI's TMPDIR is outside the workspace.

**Fix:** hoist the containment `case` above the `--selftest` block so both temp
roots are covered by one check.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 27
status: deferred
severity: P3
agent: architecture-reviewer
category: convention
confidence: 50
action_class: judgment
file: todos/review-2026-08-02-231333.md
lines: 148-150
title: §9f's control→mutant sentence gained two more durable paraphrases

**Problem:** Both records gloss the rule inline. Each is a correct citation with
a one-clause paraphrase, and a rationale record arguably needs the constraint in
hand — but CLAUDE.md's history on this exact policy is "seven longhand copies
with two divergent rationales", and this branch created two more in one pass.

**Fix:** if tightened, cite bare and let the reader open §9f.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 28
status: deferred
severity: P3
agent: architecture-reviewer
category: convention
confidence: 85
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 323, 362
title: CAP_RC is a declared output that nothing reads

**Problem:** Assigned and never consumed. Adversarial reads it as dead by design
(consistent with "the suite's exit code is deliberately not a signal");
architecture reads it as the "decoration" shape, and notes it is the cheapest
available strengthening of arm 1 — the guarded capture returns 124/137 when the
bound fires, which separates "returned at the bound" from "the suite exited on
its own".

**Fix:** either assert it in arm 1 or drop it; if kept, comment it as
deliberately unread.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 29
status: deferred
severity: P3
agent: architecture-reviewer
category: process
confidence: 60
action_class: judgment
file: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md
lines: 8-16
title: The marker-format question is now settled by precedent this branch created

**Problem:** The bullet says the branch left the marker untouched "in both
directions", but the same diff adds a new RESOLVED entry using one of the two
candidate formats. The in-section precedent previous-comments-reviewer said did
not exist now does, and it favours one answer.

**Fix:** recorded in the shard — the remaining call is whether to backfill
`:274`. Still the operator's, not decided here.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

### 30
status: applied
severity: P3
agent: silent-failure-hunter
category: silent-failure
confidence: 90
action_class: mechanical
file: scripts/run-mutation-sweep.sh
lines: 636
title: st_pipe_elapsed sanitization verified correct; consumers all route -1 to FAIL

**Problem:** Traced rather than assumed, at the reviewer's initiative: `-1` from
the non-numeric guard routes to FAIL in all three comparisons. No silent PASS.
Recorded as verified-clean so the next reviewer does not re-derive it.

**Fix:** none needed; the same guard shape was extended to `CAP_ELAPSED`
(finding 14).

applied_in: this branch (same commit as this file)

### 31
status: deferred
severity: P3
agent: adversarial-reviewer
category: reliability
confidence: 40
action_class: judgment
file: scripts/run-mutation-sweep.sh
lines: 361
title: Process-substitution capture is green but unproven

**Problem:** `> >(cat > "$out_file")` stays 27/27 green, and a read-after-write
race with `out=$(cat …)` is real in principle. The reviewer could not demonstrate
a wrong outcome (0/10 truncations at a 20,000-line transcript) and reported it as
unproven rather than as a finding.

**Fix:** none proposed; recorded so a future reader knows it was attempted.

deferred_to: memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md

---

**Summary:** 31 findings (4 P1, 7 P2, 20 P3)
**Next step:** Run `/cepa:triage` (batch auto-apply by default; pass `interactive` for one-at-a-time).
