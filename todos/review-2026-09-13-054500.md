---
date: 2026-09-13T05:45:00
scope: branch:fix/cap-rc-unread-output
branch: fix/cap-rc-unread-output
agents:
  - architecture-reviewer
  - silent-failure-hunter
agents_skipped:
  - security-sentinel: "no new injection surface — the diff adds a numeric comparison over a locally-produced exit status and composes no prompt"
  - reliability-reviewer: "signal absent — no queue, webhook, transaction, or external-API code in the diff"
  - previous-comments-reviewer: "signal absent — no PR existed at review time, no human threads; the residual this closes was verified inline against the file"
  - adversarial-reviewer: "signal absent — 20 additive lines inside an existing selftest arm; no new abstraction, no destructive verb, no high-stakes domain"
  - python-reviewer: "not rostered — no Python app"
  - performance-oracle: "not rostered — no queries"
  - data-integrity-guardian: "not rostered — no migrations"
  - schema-drift-detector: "not rostered — no ORM"
  - frontend-reviewer: "not rostered — no UI"
  - deployment-verifier: "not rostered — nothing deploys from this repo"
  - type-design-analyzer: "not rostered — no types"
conditional_dispatch:
  adversarial-reviewer:
    dispatched: false
    reason: "signal absent — additive test assertion, no new construct"
  reliability-reviewer:
    dispatched: false
    reason: "signal absent"
  previous-comments-reviewer:
    dispatched: false
    reason: "no prior review threads on this branch"
dispatch_models:
  architecture-reviewer: sonnet
  silent-failure-hunter: sonnet
roster_source: "fallback — no cepa.local.md in this clone (.claude/ absent); roster inferred from the repo's own review history (todos/review-2026-08-03-134500.md)"
deploy_verdict: not-evaluated
deploy_basis: "deployment-verifier not rostered — nothing deploys from this repo"
learnings_research: "unavailable — docs/solutions/ is gitignored (.gitignore:2) and absent in this cron clone"
grounding:
  status: "not configured"
brain:
  status: "not configured"
untrusted_data:
  suspect_items: 0
summary:
  total: 0
  p1: 0
  p2: 0
  p3: 0
  pending: 0
  ready: 0
  skipped: 0
  applied: 0
  deferred: 0
  completed: 0
---

# Review — fix/cap-rc-unread-output (round 1, clean)

**Zero findings.** Both dispatched agents independently traced the same four
questions and reported none.

This is a clean pass, not an unread diff. What each agent verified:

**architecture-reviewer** — the assertion discriminates rather than correlating:
the elapsed-time assertion above it is satisfied by both "the bound fired" and
"the fixture broke and exited fast", and only the status separates them. House
style matches (`ok=0` = PASS, observed-vs-expected detail, no diagnosis).
`124|137` introduces no new platform pin — the file already names GNU `timeout`
behaviour at three other sites. Placement is correct: time and status together
are the "the bound fired" claim, and both precede the truncation checks that
test its effect.

**silent-failure-hunter** — `CAP_RC` cannot be unbound: it is initialised at
line 395, and arm 1's call is **not** subshell-wrapped, so any of
`capture_controls`' four `FATAL … exit 2` paths ends the whole process loudly
rather than falling through to a stale read. (The two later call sites *are*
wrapped, deliberately, because they test that guard.) No stale value can leak —
arm 1 is the first live invocation in the selftest block. The `case`
wildcard correctly fails 0/1/2/125/126/127. The assertion is load-bearing: a
fixture that stopped hanging leaves the elapsed assertion green and turns
`st_file_rc` into 0, which only this assertion catches.

## Verification evidence

| Check | Command | Result |
|---|---|---|
| Syntax | `bash -n scripts/run-mutation-sweep.sh` | exit 0 |
| Selftest, green | `bash scripts/run-mutation-sweep.sh --selftest` | `39/39`, exit 0 |
| Selftest, red proof | accepted set temporarily changed to a status `timeout` cannot return | new assertion FAILs, `38/39`, exit 1 — then restored |
| Model pins | `bash scripts/check-model-pins.sh` | `0 MISS, 0 WARN`, exit 0 |

Not run, deliberately: `scripts/check-model-pins-controls.sh` (required only
when the checker changes — it did not) and the full `run-mutation-sweep.sh`
(~43 min, and CLAUDE.md forbids tracked-file edits during a sweep, which a
review loop guarantees; the weekly job covers it).

## Residual risks

None arising from this diff. The open residuals in
`memory/tasks.d/2026-08-03-fix-hang-path-executable-guard.md` are untouched and
remain `judgment` class.
