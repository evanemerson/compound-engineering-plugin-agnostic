---
date: 2026-07-11T23:36:29
scope: plan:docs/plans/2026-07-11-graphify-grounding-provider.md
branch: feat/graphify-grounding-provider
agents:
  - plan-review:coherence
  - plan-review:feasibility
  - plan-review:scope-guardian
  - plan-review:security-lens
  - plan-review:adversarial
summary:
  total: 32
  p1: 5
  p2: 14
  p3: 13
  pending: 0
  ready: 0
  skipped: 0
  applied: 32
  deferred: 0
agents_skipped:
  - agent: plan-review:product-lens
    rule: "origin validated (user-selected track #4 + ADOPT-WITH-CAVEATS spike verdict + user chose 'Build Phase 7 now') — premise settled upstream"
conditional_dispatch:
  - agent: plan-review:scope-guardian
    dispatched: true
    signal: "6 units across 4 subsystems + new canonical skill"
  - agent: plan-review:security-lens
    dispatched: true
    signal: "third-party tool integration, §7 relay surfaces, external binary in headless loops, HIPAA compliance rule"
  - agent: plan-review:product-lens
    dispatched: false
    reason: "origin validated"
  - agent: plan-review:adversarial
    dispatched: true
    signal: "first external-binary channel into the agent pipeline + new canonical-skill abstraction + unattended execution; premise techniques suppressed (validated origin)"
agents_failed: []
dropped_below_anchor: 0
validation_drops: []
deploy_verdict:
  verdict: not-evaluated
  basis: "plan review — no deploy surface"
  conditions: []
detection_signals:
  matched_docs: 3
  docs_with_detection: 3
  signals_passed: 13
  sources:
    - docs/solutions/security-issues/relaying-solution-doc-content-into-agent-prompts-needs-untrusted-data-guards.md
    - docs/solutions/logic-errors/autonomous-gates-must-fail-closed-on-missing-artifacts.md
    - docs/solutions/logic-errors/scheduled-pipelines-must-commit-their-own-state-before-cleanliness-gates.md
  backfill_candidates: []
  suspect_bullets: 0
  corpus: present
learnings_research: "ok"
---

# Review Findings — 2026-07-11 23:36 (PRE-BUILD plan review — Phase 7 graphify grounding provider)

Five personas, pre-build, 32 raw findings, 0 validation drops, 0 dropped
below anchor, 0 persona failures. All 32 applied as one plan revision
(plan gitignored in this repo — revision local-only; this file is the
tracked record). Two P1 clusters were resolved EMPIRICALLY during
synthesis (tool installed on this machine + contexthub spike graph
available): (a) `graphify update` has NO `--code-only` flag — the plan's
original sanctioned refresh would have failed on every run in every repo;
(b) `update` PRESERVES the human-built semantic layer (concept nodes and
semantic-pattern content identical across contexthub's pre/post-update
graphs, v0.9.12) — refresh-destroys-semantics refuted with evidence.
Notable synthesis catch: coherence flagged the package name `graphifyy`
as a confidence-100 typo — it is the real PyPI name (binary: `graphify`);
applied-differently as a plan Naming note so future reviewers don't
"fix" it.

## Findings (consolidated — corroborated groups merged)

### 1
- status: applied
- severity: P1
- agent: adversarial + security-lens + feasibility + coherence
- category: Fail-open guard
- confidence: 100
- action_class: corroborated
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Combined git check-ignore passes on ANY match — the ignore guard fails open

**Problem:** `git check-ignore graphify-out graph.json` exits 0 when ANY
listed path is ignored (verified in scratch repos by two personas
independently); a half-ignored repo passes the exact guard written to
prevent autonomous loops from dirtying the tree — sweep's boundary then
demotes every remaining item to report-only on every scheduled run.
**Fix applied:** Assumption 3 rewritten — per-path `git check-ignore -q`
legs that must ALL pass, failing leg named in Run Metadata; graph.json
presence checked with Glob (never Bash — no allowed-tools exposure);
nested `graphify-out/graph.json` location verified empirically (no root
artifact). (Merges adv F1, SL F3, fea F3+F4, coh F1.)

### 2
- status: applied
- severity: P1
- agent: feasibility + adversarial
- category: Untested refresh composition
- confidence: 100
- action_class: corroborated
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Sanctioned refresh command untested; semantic-layer survival unproven

**Problem:** `graphify update --code-only` was never executed in the
spike, and whether `update` destroys the human-built semantic index was
unknown — the plan's two promised jobs had an untested composition at
their core.
**Fix applied (empirical, this machine, v0.9.12):** `update` has NO
`--code-only` flag (`graphify --help`) — sanctioned refresh re-pinned to
plain `graphify update <path>` ("no LLM needed", zero network in spike
T6); contexthub pre/post-update graphs show concept/semantic nodes fully
preserved (17 concept nodes, 32 semantic-pattern hits, identical) —
refresh does not destroy enrichment. Evidence recorded in Assumption 2 +
new Verification Contract item 7 (PR body carries the excerpts).
(Merges fea F1+F6, adv F3.)

### 3
- status: applied
- severity: P1
- agent: adversarial
- category: Headless robustness
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: No timeout or stdin guard — a hung binary wedges cron sweeps indefinitely

**Fix applied:** Assumption 7 — every invocation runs
`timeout 60 graphify <verb> ... < /dev/null`; timeout/nonzero on update =
refresh failure, on query verbs = provider degraded for the rest of the
run; allowed-tools rewritten as timeout-wrapped enumerated patterns.
Judgment-class application per this repo's established rubric note
(spelled-out, safety-tightening, user-directed phase work; PR is the
review surface).

### 4
- status: applied
- severity: P1
- agent: security-lens
- category: Shell injection
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Repo-derived symbols spliced into pre-authorized graphify command lines

**Problem:** a hostile diff can define a symbol containing `$(...)`;
`Bash(graphify affected:*)` prefix-matches regardless of argument
content, so a headless run executes the substitution unprompted — §7's
never-splice rule never carried to the new shell-out surface.
**Fix applied:** Assumption 7 sanitization — arguments composed from
extracted identifiers only, charset allowlist, violating candidates
skipped and counted (`args_skipped` in the U4 block); raw diff hunks or
doc text never passed as arguments.

### 5
- status: applied
- severity: P2
- agent: scope-guardian
- category: Scope drop
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Learnings-researcher grounding wiring silently narrowed to review-only

**Problem:** the pre-step activated only when the invoker announced
availability, and only review.md announced — dead code in exactly the
task/lfg loops where the spike aimed the semantic index.
**Fix applied:** U3 extended to task.md (research phase) and lfg.md
(Step 2.1) — one availability-check + handoff paragraph each; both are
pipeline commands (no allowed-tools work, per the CLAUDE.md exception).

### 6
- status: applied
- severity: P2
- agent: feasibility
- category: Sequencing
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Grounding block placed in a section that executes after agents launch

**Fix applied:** U2 — grounding check/refresh/queries execute at the TOP
of Step 3, before the learnings-researcher dispatch; the `grounding:` key
stays documented in Integration Dispatch with an explicit
cross-reference to the earlier execution point.

### 7
- status: applied
- severity: P2
- agent: feasibility + adversarial (+ security-lens researcher-side)
- category: Unclassified failure path
- confidence: 100
- action_class: corroborated
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Mid-run verb failure after availability passes had no classification

**Fix applied:** Assumption 7 degrade rule — any sanctioned verb failing
after availability passes degrades the provider for the remainder of the
run (`status: unavailable — <verb> failed: <reason>`), already-relayed
output stands; new U1 scenario 6. Researcher-side: pre-step failures
reported in the briefing (`grounding pre-step: failed — <reason>`) and
copied into Run Metadata by the invoker. (Merges fea F5, adv F5, SL F5.)

### 8
- status: applied
- severity: P2
- agent: adversarial
- category: Unbounded cost/output
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Query budget had no number; per-query output could flood reviewer prompts

**Fix applied:** 5 queries per run, SHARED across orchestrator and
researcher (U4's `queries:` = that total); `query` always carries native
`--budget 2000` (flag verified in `graphify --help`); `affected` keeps
default depth 2; 100-line relay truncation with explicit
`[truncated: N lines omitted]` marker beside the §7 clause.
(Merges adv F6+F7.)

### 9
- status: applied
- severity: P2
- agent: security-lens
- category: Durable record
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Researcher-level strips had no durable-record path (two strip sites, one counter)

**Fix applied:** Assumption 5 — `grounding.suspect_stripped` sums
orchestrator relay strips AND researcher-reported pre-step strips; one
corrupted-input finding per strip filed under grounding, never under
`detection_signals` (prevents miscounting against the Detection pipeline).

### 10
- status: applied
- severity: P2
- agent: security-lens
- category: Compliance (carve-out)
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Maintaining graphify-out/ arms the user's global graphify skill's LLM doc pass in compliance repos

**Problem:** cepa's own conduct is safe (no LLM pass ever), but the
artifacts cepa refreshes make the globally-installed graphify skill's
trigger condition true — and its doc pass ships templates/docs to an LLM,
outside cepa's sanctioned-verb enforcement.
**Fix applied (warning-tier; policy hardness deferred to user):** U1
compliance rule + U5 setup flag (grounding + `## Compliance` combination
gets the warning) + U6 README compliance paragraph. The
harden-to-error option and version-pinning question are recorded in the
plan's Deferred section as the user's call — compliance carve-out
respected: nothing here loosens any guard.

### 11
- status: applied
- severity: P2
- agent: security-lens
- category: allowed-tools
- confidence: 75
- action_class: judgment
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: allowed-tools patterns pre-authorized more than the sanctioned invocation set

**Problem:** `Bash(graphify update:*)` matched the unflagged refresh
variant (then believed non-sanctioned), and the sanctioned `path` verb
had no consumer or pattern — sanctioned set and pre-authorized set
diverged in both directions.
**Fix applied:** Assumption 8 — timeout-wrapped enumerated patterns
exactly matching the four sanctioned verbs; `path` dropped from the
sanctioned set (no consumer; sets now identical). Partly superseded by
finding 2's empirical result (no flag exists), which the rewrite absorbs.
(Merges SL F2, coh F6, fea F8.)

### 12
- status: applied
- severity: P3 (batch — mechanical record fixes + anchor-50 advisories)
- agent: panel (various)
- category: Record accuracy
- confidence: 50-75
- action_class: mechanical
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: Remaining record fixes applied in the revision

**Batch:** Dependencies lines on U2-U6 (coh F5 + adv F4, corroborated);
U6 + U4 `Test expectation: none` annotations (coh F3); Verification
Contract item 6 traces ALL 15 scenarios (coh F4); `extract` (any form)
added to the forbidden list and the item-5 audit list, initial build
declared a human action (adv F8); spike §6 risks #3/#4/#6 restored to
the Deferred ledger as rollout gates (scope F2); U4 `refreshed:`
annotated code-layer-only + semantic-lag relay rule (scope F3);
configured-repos-must-emit rule — absent block is a recording defect,
not a not-configured signal (SL F7); graph.json location ambiguity
resolved empirically — nested, no root artifact (fea F7).

### 13
- status: applied
- severity: P3
- agent: coherence
- category: False-positive converted to guard
- confidence: 100
- action_class: mechanical
- file: docs/plans/2026-07-11-graphify-grounding-provider.md
- title: "graphifyy" flagged as a typo at confidence 100 — it is the real package name

**Problem:** the PyPI package is genuinely `graphifyy` (binary:
`graphify`) per spike T1; a confidence-100 "fix" would have shipped a
broken install command — and proves the ambiguity will trip future
readers.
**Fix applied (differently):** Naming note added to the plan's Goal
section and U5's report text ("package name deliberately `graphifyy`");
recorded here so future reviews don't re-flag it.

---

**Summary:** 32 findings (5 P1, 14 P2, 13 P3) — consolidated into 13 entries above; all applied to the plan revision
**Next step:** Build to the revised plan.
