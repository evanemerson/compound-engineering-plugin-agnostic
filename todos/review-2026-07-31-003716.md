---
date: 2026-07-31T00:37:16
scope: branch:fix/cite-trunk-ladder-once
branch: fix/cite-trunk-ladder-once
agents:
  - adversarial-reviewer
summary:
  total: 2
  p1: 0
  p2: 1
  p3: 1
  pending: 0
  ready: 0
  skipped: 0
  applied: 2
  deferred: 0
  completed: 0
agents_skipped:
  - agent: silent-failure-hunter
    reason: "round 3 scope is the round-2 diff (a regex tail, two grep flags, one guard predicate, two prose reflows). Round 2's silent-failure pass verified all six round-1 fixes and found the two false-positive classes now fixed; the adversarial dispatch below carries the silent-pass hunt for this round explicitly, and found the one real hole"
  - agent: security-sentinel
    reason: "no new expansion, external input, or shell-out in the round-2 or round-3 diffs; round 1 traced every expansion with live payloads"
  - agent: architecture-reviewer
    reason: "round 3 scope is one script's internals plus one §9f table row"
  - agent: previous-comments-reviewer
    reason: "record-integrity re-verification folded into the adversarial dispatch, as in round 2"
  - agent: performance-oracle
    reason: "not in cepa.local.md roster"
  - agent: python-reviewer
    reason: "not in roster"
  - agent: data-integrity-guardian
    reason: "not in roster"
  - agent: schema-drift-detector
    reason: "not in roster"
  - agent: frontend-reviewer
    reason: "not in roster"
  - agent: deployment-verifier
    reason: "not in roster"
  - agent: pr-test-analyzer
    reason: "not in roster — no test suite"
  - agent: type-design-analyzer
    reason: "not in roster"
conditional_dispatch:
  - agent: adversarial-reviewer
    dispatched: true
    reason: "rounds 1 and 2 each found real defects in code written during this run; the evidence supported a third pass rather than stopping on a clean-looking round 2"
  - agent: reliability-reviewer
    dispatched: false
    reason: "no queue/webhook/transaction/lock/cache/external-call code in the diff"
  - agent: previous-comments-reviewer
    dispatched: false
    reason: "folded into the adversarial dispatch; results in the verified-clean section"
deploy_verdict:
  verdict: not-evaluated
  basis: "deployment-verifier not in roster — plugin source repo, no deploy target"
  conditions: []
detection_signals:
  matched_docs: 1
  docs_with_detection: 1
  signals_passed: 5
  sources:
    - docs/solutions/logic-errors/unpinned-subagent-dispatches-inherit-the-session-model.md
  backfill_candidates: []
  suspect_bullets: 0
  corpus: present
learnings_research: "ok — reused the briefing dispatched at lfg Step 2."
brain:
  provider: ob1
  status: fresh
  role: consumer
  queries: 0
  written: 0
  suppressed_writebacks: 0
  scrubbed: 0
  args_skipped: 0
  suspect_stripped: 0
  non_participant_dropped: 0
  pre_step: "ok — health and registry resolved at Step 2; no query spent this round."
---

# Review Findings — 2026-07-31 00:37 (round 3)

**Subject:** branch `fix/cite-trunk-ladder-once`, commit `e6875aa` — the
round-2 fixes.

**Through-line:** all six round-2 fixes verified real, including a set-level
comparison confirming the narrowed citation regex dropped nothing (36/36 raw
matches, 30/30 expanded pairs, empty symmetric difference both times). Record
integrity reconciled exactly across all six findings files — no stale tail this
round, the first round that has been true.

The one substantive finding is a defect **my round-2 fix introduced**: I widened
three of the script's four file-selection globs. That is the construct-vs-instance
class CLAUDE.md documents (allowed-tools twice, hardcoded counts three times,
model pins three times) — and it is the *fourth* time this run has produced an
instance of it. The fix therefore closes the construct: one declared extension
set, both `find` and `grep` syntaxes derived from it, all four sites pointing at
it, so the next widening cannot land at three of four.

---

### 1
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: correctness
- confidence: 100
- action_class: mechanical
- reclassified: "2026-07-31 — agent emitted judgment on the grounds that it widens legs 2-3's discovery set, which §9f says lands alone. Reclassified: the widening is not new scope — it restores parity with leg 1's existing construct and closes a silent-pass hole this run's own round-2 commit opened. §9f's requirement is the re-run, which was performed (full leg-1/2/3 regression plus the leg-4 suite)."
- file: scripts/check-model-pins.sh
- lines: 313 (pre-fix)
- title: Round 2 widened three of the script's four file-selection globs, leaving a `.markdown` file readable by leg 4 and invisible to the legs that check dispatch pins
- applied_in: "this branch — one declared extension set, both syntaxes derived from it, all four sites pointing at it"

**Problem:** Round-2 finding #5 was scoped as "`--include='*.markdown'` added to
both glob sets." The script has **four** file-selection sites, not two:

| site | before round 3 |
|---|---|
| leg 1 discovery | `-name '*.md' -o -name '*.markdown'` — both |
| leg 4 citation scan | widened by round 2 |
| leg 4 coverage probe | widened by round 2 |
| **legs 2-3 discovery** | **`-name '*.md'` only — untouched** |

Traced and reproduced. A file `plugins/cepa/commands/zztest.markdown` carrying
an unpinned dispatch instruction and an **inverted** mode-conditional marker
(`interactive=haiku headless=opus`) was read by leg 4 — confirmed by planting a
bogus citation in the same file, which MISSed — and never reached `scan_body`
or `scan_conditional`. Identical content, extension flipped:

```
zztest.md        -> -- 1 MISS, 1 WARN --
zztest.markdown  -> -- 0 MISS, 0 WARN --   (before the fix)
```

So an unattended run configured to cost more than the attended one it mirrors
shipped as full compliance. This is precisely the failure the script's own
scan-roots comment records having found and fixed once ("A mode-conditional
marker placed in an unscanned file was checked by nothing and read as full
compliance"), recurring through a different door.

Latent today — zero `.markdown` files exist — which is why it survived three
rounds: the trigger is someone adding one file.

**Fix:** Declare the extension set once (`MD_EXTS='md markdown'`) and build both
the `find -name` and the `grep --include` argument arrays from it, since the two
tools want different syntax for the same fact. All four sites now consume the
derived arrays. Verified after the fix: identical content in both extensions
produces identical output (`1 MISS, 1 WARN` each); legs 1, 2 and 3 still catch
an unsanctioned tier, an inverted pair and a dropped headless branch; the leg-4
control suite is unchanged; baseline 0 MISS / 0 WARN, 30 pairs, 5 of 5 roots.

---

### 2
- status: applied
- severity: P3
- agent: adversarial-reviewer
- category: documentation-accuracy
- confidence: 100
- action_class: mechanical
- file: plugins/cepa/skills/autonomy/SKILL.md
- lines: 737
- title: The §9f row restored in round 2 named one symptom and prescribed a remedy that does not hold for the sibling shape live in README.md today
- applied_in: "this branch — the row now states the mechanism and both realizations"

**Problem:** Round 2 restored the dropped limit as "a qualifier separated from
its anchor by a line break," with reflow as the remedy. The mechanism is
narrower than that: leg 4 captures **exactly one token immediately preceding the
anchor**. A line break is one way to separate them; an intervening word is
another, and reflow does nothing for it.

`README.md:420-421` is the live case — "all live in the `cepa:autonomy`
contract" / "(§2b):". Reflowed onto one line, as the row prescribed, the token
adjacent to the anchor becomes `contract`, so the citation still takes the
permissive unqualified branch. Reduced case, one line, one intervening word:

```
The rule is in `cepa:brain` §9c.                  -> 1 MISS   (recorded)
The rule is in the `cepa:brain` contract (§9c).   -> 0 MISS   (unrecorded)
```

A reader applying the recorded remedy to a citation of that shape gets a green
build and believes the owner check was restored when it was never engaged. The
row's own history — round 2's finding #3 was that this table *lost* a limit it
was consolidating — is the argument for stating it precisely.

**Fix:** State the trigger as the mechanism (the owner name is not the token
immediately before the anchor) and name both realizations, noting reflow fixes
the line-break shape only and that the intervening-word shape is live in
`README.md`. Keep the "two live instances reflowed" sentence scoped to the shape
it describes.

---

## Verified clean (round 3)

- **All six round-2 fixes hold**, each independently reproduced.
- **The narrowed `CITE_RE` dropped nothing** — raw match sets and expanded pair
  sets compared before and after: 36/36 and 30/30, empty symmetric difference
  both times. It still expands real ranges and no longer invents anchors from
  hyphenated English.
- **The `rfiles` probe is correct on every root shape** — directory roots and
  the two single-file roots (GNU grep's `--include` does filter command-line
  file arguments), files with no trailing newline, and NUL-bearing files. Its
  placement inside `rrc == 1` is right: `rrc == 0` entails at least one
  includable file was read.
- **`-a` on the index grep is load-bearing** — with a NUL in `autonomy/SKILL.md`
  the pre-fix grep returned 5 of 7 anchors; with `-a`, all 7.
- **The reflowed citations render identically** (soft breaks inside a paragraph)
  and now produce genuinely qualified pairs. A repo-wide scan for the same shape
  returns exactly 2 instances at `98e66be` and **0** at HEAD — the "two live
  instances" claim is exact, with none missed.
- **Record integrity reconciles exactly across all six findings files** —
  frontmatter totals, severity distributions, status distributions, and `### N`
  heading counts. No stale prose tail this round.
- **Manifests both at 1.16.0**; counts rule untriggered; no stale old-path
  reference in any tracked file.
- **Everything else attacked survived**: hostile input into the qualifier
  charset, range-expansion contamination across matches, the `|` delimiter,
  `rrc` capture ordering (the `rfiles` assignment clobbers `$?` only after
  `rrc` is captured), empty-`rout` concatenation, the `checked` backstop, and
  the reflowed lines' interaction with leg 2's block scoping.

---

**Summary:** 2 findings (0 P1, 1 P2, 1 P3) — 2 applied, 0 deferred
**Review loop:** 3 rounds run, the maximum. Rounds 1 and 2 each found real
defects in code written during this run, which is what justified continuing;
round 3 found one more, introduced by round 2's own fix. Residuals from all
three rounds are in
`memory/tasks.d/2026-07-30-fix-cite-trunk-ladder-once.md` and the PR body.
**Next step:** Run `/cepa:triage` on the remaining deferrals, or take the
numbered follow-ons in the PR body.
