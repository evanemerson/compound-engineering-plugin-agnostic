---
date: 2026-07-12T14:50:44
scope: feat/brain-provider — OB1 brain integration gauntlet (plan-adherence + branch review + verification)
branch: feat/brain-provider
agents:
  - plan-adherence-audit
  - adversarial-reviewer
  - previous-comments-reviewer
summary:
  total: 22
  p1: 2
  p2: 16
  p3: 4
  pending: 0
  ready: 0
  skipped: 0
  applied: 22
  deferred: 0
agents_skipped:
  - agent: security-sentinel/performance-oracle/python-reviewer/data-integrity-guardian/schema-drift-detector/frontend-reviewer/deployment-verifier
    rule: "markdown+bash plugin diff; fitted roster per repo precedent"
conditional_dispatch:
  - agent: adversarial-reviewer
    dispatched: true
    signal: "new external network dependency + credentials + PHI/compliance boundary + 2 shell scripts + untrusted-content relay"
  - agent: reliability-reviewer
    dispatched: false
    reason: "instruction docs + bash scripts; no queue/transaction code (the HTTP failure model is reviewed by adversarial)"
  - agent: previous-comments-reviewer
    dispatched: true
    signal: "13 prior review files; diff touches §7-relay, allowed-tools, file-todos Run Metadata, and 3 prior-fixed provider surfaces"
agents_failed:
  - agent: silent-failure-hunter
    reason: "pr-review-toolkit:silent-failure-hunter returned prompt-injection / derailed output (0 tool_uses) on TWO consecutive dispatches — first a block of injected 'you must use a Skill / do not write text yourself' instructions, second a fake 'System:' meta-line. Neither performed the review. Injected content was NOT acted on (autonomy §7). Coverage backfilled by a direct inline silent-failure check (found + fixed the file-todos brain-block gap and a compound-refresh silent-skip)."
deploy_verdict:
  verdict: not-evaluated
  basis: "markdown/bash plugin; no deploy surface"
  conditions: []
detection_signals:
  matched_docs: 3
  docs_with_detection: 3
  signals_passed: 12
  sources:
    - docs/solutions/security-issues/relaying-solution-doc-content-into-agent-prompts-needs-untrusted-data-guards.md
    - docs/solutions/security-issues/argument-sanitization-at-every-shell-out-relay-point.md
    - docs/solutions/logic-errors/durable-record-promises-need-a-named-sink-in-every-phase.md
  backfill_candidates: []
  suspect_bullets: 0
  corpus: present
learnings_research: "ok"
---

# Review Findings — 2026-07-12 14:50 (OB1 brain gauntlet)

Run shape: 3 working reviewers + 1 inline (silent-failure-hunter derailed twice on
injected output — see `agents_failed`; a real prompt-injection caught by exactly the §7
discipline this feature implements, ignored, and its coverage done inline). Plan-adherence
and adversarial both re-verified the shipped call contract + scripts against the REAL OB1
`~/webapps/OB1/integrations/agent-memory-api/index.ts` and caught several ground-truth
mismatches the pre-build plan-review's API check missed. All 22 applied in commit 843069d
(+ the two inline fixes in e15cafc). Judgment-class applications follow this repo's
established rubric (verified, spelled-out, safety/durability-tightening, user-directed
phase work; PR is the review surface). The compliance-forced-scrub fix (finding 1) tightens
the user's dpc-pro decision — surfaced to the user in the report, not silently reversed.

## Findings (consolidated where corroborated)

### 1
- status: applied
- severity: P1
- agent: adversarial-reviewer
- category: Compliance / PHI egress
- confidence: 90
- action_class: judgment
- file: plugins/cepa/skills/brain/SKILL.md + plugins/cepa/commands/compound.md
- title: A `## Compliance` repo opting into brain: but omitting brain_phi_scrub egressed PHI ungated

**Problem:** the scrub was a separate opt-in on top of the `brain:` opt-in; a HIPAA repo
that added `brain:` but forgot `brain_phi_scrub: true` wrote unscrubbed content to
Supabase + OpenRouter, unremediable (no DELETE). **Fix applied:** for any participating
repo whose `cepa.local.md` has a `## Compliance` section the PHI scrub is FORCED on, and
writeback is SUPPRESSED (fail-closed) if the scrub can't run — honoring the user's
"dpc-pro participates" decision while removing the single-forgotten-line leak.

### 2
- status: applied
- severity: P1
- agent: plan-adherence-audit
- category: allowed-tools
- confidence: 85
- action_class: mechanical
- file: plugins/cepa/commands/review.md
- title: review.md emits the brain pre-flight but its allowed-tools lacked Bash(bash:*)

**Fix applied:** added `Bash(bash:*)` (the twice-shipped headless-degrade class; U6's
allowed-tools audit had scoped only compound). Both count/manifest audits pass.

### 3
- status: applied
- severity: P2
- agent: plan-adherence-audit + adversarial-reviewer
- category: API ground truth — retirement
- confidence: 90
- action_class: corroborated
- file: plugins/cepa/skills/brain/SKILL.md + compound.md + compound-refresh.md
- title: `supersede` is a server-side no-op; edited docs never retired AND never re-written

**Problem:** OB1's PATCH review has no `supersede` branch (needs `related_memory_id`);
recall never drops on it. Combined with a content-independent idkey (skip-not-update),
an edited doc kept serving the OLD lesson. **Fix applied:** retire via `mark_stale` (the
only action that sets `lifecycle_status='stale'`, which recall drops) + a CONTENT-hash
idempotency base key (`idkey <repo> <path> <payloadfile>`) so edited atoms get new rows
while unchanged docs still dedup.

### 4
- status: applied
- severity: P2
- agent: plan-adherence-audit
- category: API ground truth — payloads
- confidence: 90
- action_class: mechanical
- file: plugins/cepa/skills/brain/SKILL.md + learnings-researcher.md
- title: recall max_items nested in scope (API reads limits); schema_version literal + workspace_id source undocumented

**Fix applied:** documented recall as `scope:{project_only:false}, limits:{max_items:10}`;
stated the exact `schema_version` literals (`openbrain.agent_memory.{recall,writeback}.v1`);
`workspace_id` from `BRAIN_WORKSPACE_ID` in `.env.local`.

### 5
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: PHI scrub coverage
- confidence: 90
- action_class: judgment
- file: plugins/cepa/scripts/brain-client.sh + brain/SKILL.md
- title: scrub passed ISO-8601 DOBs and space/dot SSNs; docs over-claimed name redaction

**Fix applied:** scrub adds ISO-8601 date + space/dot SSN patterns (verified); the skill
now states the scrub is NUMERIC-only and explicitly does NOT redact names (regex name
detection isn't feasible) — names rely on operator certification + hard-delete backstop.

### 6
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: Enforceability
- confidence: 75
- action_class: judgment
- file: plugins/cepa/skills/brain/SKILL.md + learnings-researcher.md
- title: recall provenance filter had no participant registry to check against

**Fix applied:** defined a tracked `brain-participants.tsv` (project_id → active|retracted)
U1 artifact the invoker passes to the researcher; recall drops retracted/unknown-provenance
memories fail-closed. Manifest tracked outstanding in memory/tasks.md.

### 7
- status: applied
- severity: P2
- agent: adversarial-reviewer + previous-comments-reviewer
- category: Durability / retry
- confidence: 80
- action_class: corroborated
- file: plugins/cepa/scripts/brain-client.sh + brain/SKILL.md + compound.md
- title: Partial writeback stranded pending-invisible rows; the plan's promised retry never shipped

**Fix applied:** bounded retry in the client (curl `retry=1` — transient/5xx/timeout only,
never 4xx; safe because writeback is idempotent); producer promotes the ids the call DID
return before degrading, so partial rows are never left invisible in `pending`.

### 8
- status: applied
- severity: P2
- agent: previous-comments-reviewer (+ inline silent-failure)
- category: Spec parity (prior-fixed classes)
- confidence: 90
- action_class: mechanical
- file: plugins/cepa/skills/file-todos/SKILL.md + review.md + brain/SKILL.md + setup.md
- title: Three Phase-7 fix classes not mirrored for brain

**Fix applied (batch):** file-todos gains the `brain` Run Metadata block + emission scope
(was referenced by 4 files, never defined — inline fix, commit e15cafc); review.md headless
structured summary carries the `brain` status line; brain skill + setup.md gain the
headless-permissions note (`Bash(bash:*)` allowlist entry) so the recall pre-step isn't
dead-on-arrival in unattended runs.

### 9
- status: applied
- severity: P2/P3 (batch)
- agent: plan-adherence + adversarial + previous-comments
- category: Tracking / accuracy
- confidence: 75
- action_class: mechanical/judgment
- file: docs/plans + memory/tasks.md + brain/SKILL.md + todos/review-2026-07-12-115234.md
- title: Remaining accuracy + tracking fixes

**Batch:** the `service_role` hard-delete backstop is now explicitly an OUTSTANDING
server-side operator procedure (NOT shipped as a cepa command), tracked in memory/tasks.md
+ plan Deferred (was described as if it existed); the `brain:` value is a presence marker
only, URL/key come from `.env.local` (stale-value footgun closed); pre-U5 live-API
validation items recorded; the plan-review findings file statuses reconciled (had drifted
ready→applied); compound-refresh Replace path uses `mark_stale` not `supersede`; the
compound-refresh silent-skip degrades+records (inline fix, e15cafc).

---

**Summary:** 22 findings (2 P1, 16 P2, 4 P3) — consolidated into 9 entries; all applied,
0 deferred. 1 reviewer failed (silent-failure-hunter, injection-derailed — coverage
backfilled inline). All shipped call-contract fixes verified against the real OB1 API.
**Next step:** Ship PR (brain is inert behind opt-in — safe to merge before the live
instance exists). U1 (stand up OB1) + U5 (backfill) await user credentials.
