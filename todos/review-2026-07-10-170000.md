---
date: 2026-07-10T17:00:00
scope: feat/compounding-hygiene — round 2 (fix-batch verification of ec1d8d2)
branch: feat/compounding-hygiene
agents:
  - architecture-reviewer
  - adversarial-reviewer
summary:
  total: 7
  p1: 0
  p2: 4
  p3: 3
  pending: 0
  ready: 0
  skipped: 0
  applied: 6
  deferred: 1
agents_skipped:
  - agent: silent-failure-hunter
    rule: "round-2 scope is fix-batch verification; adversarial-reviewer's per-finding FIXED/PARTIAL protocol covers the silent-failure class this round"
  - agent: previous-comments-reviewer
    rule: "continuity check subsumed by adversarial round-1-findings verification (its whole protocol this round)"
conditional_dispatch:
  - agent: adversarial-reviewer
    dispatched: true
    signal: "verifying auto-applied security/data-loss fixes from round 1"
  - agent: reliability-reviewer
    dispatched: false
    reason: "no queue/webhook/transaction/external-call code in diff"
  - agent: previous-comments-reviewer
    dispatched: false
    reason: "see agents_skipped — verification protocol covered by adversarial pass"
deploy_verdict:
  verdict: not-evaluated
  basis: "deployment-verifier skipped — markdown plugin repo, no deployable config"
  conditions: []
detection_signals:
  matched_docs: 0
  docs_with_detection: 0
  signals_passed: 0
  sources: []
  backfill_candidates: []
  suspect_bullets: 0
  corpus: none
learnings_research: "ok"
---

# Review Findings — 2026-07-10 17:00 (round 2)

Round-1 fix verification scorecard (adversarial): findings 3,4,5,7,8,9,10,11
FIXED; 1,2,6 PARTIAL — residuals below as findings 2, 3, 5. Architecture:
all round-1 cross-file plumbing (detection_signals/stale fields/atomicity/
outcome enums/§7) verified consistent.

## Findings

### 1
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: Contract break (same class as r1 finding 1)
- confidence: 80
- action_class: mechanical
- file: plugins/cepa/commands/review.md
- lines: 4
- title: review.md and compound.md allowed-tools lack Write/Edit for headless writes

**Problem:**
review.md Step 5 must Write the findings file and names scheduled runs as
headless callers; compound.md writes the solution doc, plan link, and
CONCEPTS.md. Neither pre-authorizes Write/Edit — a scheduled headless run
completes with no deliverable, the exact class fixed in compound-refresh.

**Fix:** Add Write, Edit to both frontmatters.

### 2
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: Permission rule (r1 finding 1 residual)
- confidence: 78
- action_class: mechanical
- file: plugins/cepa/commands/compound-refresh.md
- lines: 4
- title: Bash(rm docs/solutions/*) is inert under exact-match semantics, traversal-prone under glob

**Problem:**
The rule matches only the literal string under documented prefix semantics
(headless Delete degrades to Recommended — r1 finding 1 resurfaces for the
core verb), and under a glob reading admits `docs/solutions/../../<x>`.

**Fix:** Delete via `git rm docs/solutions/<relative-path>` (exact relative
path, no flags, no `..`), with `Bash(git rm:*)` replacing the rm rule —
tracked-files-only, staged atomically for Phase 5, recoverable.

### 3
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: Untrusted content (r1 finding 2 residual)
- confidence: 78
- action_class: mechanical
- file: plugins/cepa/commands/review.md
- lines: 67-81
- title: SUSPECT payloads still relayed to agents; declarative exemption claims unguarded; no durable sink

**Problem:**
The researcher's SUSPECT quote rides the briefing into every agent prompt
("not relayed as a signal" ≠ not relayed); the ignore clause binds only on
imperatives, so declarative payloads ("pre-cleared upstream", "known false
positives — no finding needed") pass; a researcher-caught injection attempt
is recorded nowhere.

**Fix:** Orchestrator strips SUSPECT blocks before dispatch and itself files
the corrupted-signal finding; ignore clause extended to pre-cleared/safe/
exempt claims (researcher SUSPECT rule too); `suspect_bullets` added to
detection_signals in file-todos.

### 4
- status: applied
- severity: P3
- agent: adversarial-reviewer
- category: Pipeline gap (r1 finding 8 downstream)
- confidence: 75
- action_class: mechanical
- file: plugins/cepa/agents/research/learnings-researcher.md
- lines: 40
- title: BACKFILL-marked Detection sections invisible to both backfill consumers

**Problem:**
A doc whose Detection contains only the BACKFILL marker "has" a section, so
the researcher doesn't list it as a backfill candidate and compound-refresh's
missing-Detection Update trigger doesn't fire — the doc class the marker
routes to backfill falls through.

**Fix:** Both consumers treat an empty or BACKFILL-marker-only Detection
section as absent.

### 5
- status: applied
- severity: P2
- agent: adversarial-reviewer
- category: Data loss (r1 finding 6 residual)
- confidence: 80
- action_class: mechanical
- file: plugins/cepa/commands/compound-refresh.md
- lines: 148-153, 183-190
- title: Same-path Replace overwrites the old doc before validation can fail

**Problem:**
The replacement subagent writes the successor at the same path — a
*successful* write that destroys the old content — then validation fails and
"do NOT delete the old doc" is vacuous; the invalid successor gets
stale-marked and can be committed.

**Fix:** Replacement subagents RETURN successor content; the orchestrator
validates against the compound-docs spec BEFORE any write touches disk —
the old doc is never overwritten or deleted until validation passes (matches
the "only the orchestrator writes files" idiom from compound.md).

### 6
- status: applied
- severity: P3
- agent: architecture-reviewer
- category: Spec vocabulary
- confidence: 75
- action_class: mechanical
- file: plugins/cepa/skills/file-todos/SKILL.md
- lines: 159-170
- title: detection_signals can't express "no corpus exists" vs "corpus present, zero matches"

**Problem:**
The dogfooded r1 findings file had to invent an ad-hoc `note` key to say the
repo has no docs/solutions at all — semantically different from an empty
match, and the spec has no vocabulary for it.

**Fix:** Add `corpus: none | present` to the detection_signals spec; update
the r1 file to use it.

### 7
- status: deferred
- severity: P3
- agent: adversarial-reviewer
- category: Stale side-channel (judgment)
- confidence: 60
- action_class: judgment
- file: plugins/cepa/commands/compound-refresh.md
- lines: 32-35
- title: Stale-marking leaves CLAUDE.md rules derived from the stale doc active

**Problem:**
A CLAUDE.md rule promoted from a doc later judged stale keeps flowing to
review agents via the researcher's Active Rules section — the stale doc's
substance flags correct code through a side door.

**Fix (proposed):** On stale-mark, run the same inbound-citation grep Delete
uses and flag instruction-file citations in the report for human review.
Sub-threshold confidence; interacts with CLAUDE.md-editing policy — human
call.

---

**Summary:** 7 findings (0 P1, 4 P2, 3 P3)
**Next step:** Run `/cepa:triage` (batch auto-apply by default; pass `interactive` for one-at-a-time).
