# Residuals — 2026-09-25 (main)

## 2026-09-25 — README restructure + count-bearing anchors (PRs #64, #65)

Both PRs merged. These are the follow-ups the `/cepa:compound` pass named and
this session did not build. Source:
`docs/solutions/logic-errors/a-doc-claiming-a-mechanism-must-state-how-that-mechanism-runs.md`
(local-only — `docs/` is gitignored; the tracked half is the CONCEPTS.md entry
in c9d5c13 and the brain rows).

- [x] **P3 — Add the drafted CLAUDE.md rule "Anchors must not encode a count
  that can drift."** The existing count-drift Critical Rule covers counts as
  prose numbers only. A count inside a heading is a link target whose slug
  carries it, so a drifted count 404s the link silently rather than merely
  reading stale — a different failure surface needing its own line, not a
  silent broadening of the existing rule. Full draft text is in the solution
  doc's `## Prevention`. `file: CLAUDE.md` · `action_class: judgment`
  (a new Critical Rule is the operator's call) · `confidence: 85`
  **DONE 2026-09-25** — operator approved rule-only; added directly after the
  count-drift rule it is a sibling of.
  The draft's citation was corrected on the way in: it read "found in
  README.md's own `[the full table](#commands-11)` (PR #64)", implying the link
  predated the PR. History says otherwise — `85ac5d9` *introduced* that link
  inside #64, and `df36b9f`, a review-findings commit in the same PR, removed
  it. The shipped rule cites that sequence, which is stronger evidence: the
  hazard survived a careful author mid-restructure and only human review caught
  it.

- [ ] **P3 — Build the anchor-digit checker.** The one genuinely hard-gateable
  part of the class: extract headings matching `^#{2,4} .*\(\d+\)`, apply
  GitHub's slug rule, flag any internal link whose target slug carries a bare
  integer. Same shape as `check-model-pins.sh` leg 4, which already resolves
  §N citations against headings. The other two legs of this class are NOT
  hard-gateable and the checker must say so in its header: CI-trigger-claim vs
  actual-trigger is WARN-tier only (deciding whether English characterizes a
  trigger is not mechanical), and a branch-protection claim is not gateable at
  all (mutable platform state that drifts with no commit).
  `file: scripts/` · `action_class: judgment` · `confidence: 80`

Neither blocks anything. Both are prevention work for a class already fixed at
every site it was reported at.

## Handoff pointer

- Handoff written local-only (docs/ is gitignored):
  `/home/evan/webapps/compound-engineering/docs/handoff/2026-09-25-readme-restructure-and-count-anchors.md`
  — subject: README restructure and count-bearing anchors. Session change: GO.
  Previous handoff (2026-09-13) stamped SUPERSEDED.
