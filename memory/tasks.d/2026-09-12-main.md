# Residual — main (brain-client idkey defect class)

## 2026-09-12

- Handoff document written local-only:
  `/home/evan/webapps/compound-engineering/docs/handoff/2026-09-12-brain-client-idkey-defect-class.md`
  — subject "brain-client idkey defect class". `docs/` is gitignored in this
  repo by convention (CLAUDE.md), so the file is not tracked and this pointer
  is its only tracked trace. — 2026-09-12
- Note (not a defect): the PR #47 residual shard
  `memory/tasks.d/2026-08-22-fix-brain-writeback-contract.md` records the
  brain-ops fix as "all four prompts". The real count is **seven call sites
  across five files** — four `backfill-*.prompt.md` plus three sites inside
  `2026-07-16-brain-rollout-runbook.md` (lines 84, 111, 135). Both figures were
  right about different units: four prompt *files*, seven *call sites*. The
  runbook sites were not visible from this repo when that shard was written.
  Verified read-only via `gh api` on 2026-09-12: all seven now pass the payload
  file as arg 3, zero wrong calls remain. The item stays CLOSED; this only
  corrects the count. — 2026-09-12
- Deferred: the six mutants used to validate `scripts/check-brain-client-args.sh`
  were run by hand this session via `CEPA_BRAIN_CLIENT`, not added to
  `scripts/mutants/registry.sh`. The controls suite is durable; the proof that
  the suite works is not yet repeatable. Adding them touches
  `run-mutation-sweep.sh`, which takes ~43 min locally and refuses a tree that
  changes mid-run — so it wants its own session. Not build-eligible as written:
  it needs the registry's `reg` shape decided first (the existing registry
  targets only the model-pin checker, so a second target may need a harness
  field). — 2026-09-12
