# Residual — main (agent-fleet dashboard)

## 2026-09-13

- **DONE 2026-09-13 — the agent-fleet dashboard landed in `ci-ops-self-hosted`.**
  PR #20 `feat(dashboard): a second view for the unattended agent fleet`, head
  `feat/agent-fleet-dashboard` → base `main`, MERGED 2026-09-13 13:56 UTC as
  squash commit `9824ddb0c82b740595a973d3476164a2df169480`. The prompt was run
  in that repo's own session, branch-first as written. Both gaps the hand-off
  carried are closed in the same commit: `dashboard/public/agents.json` is now
  gitignored (with its `.agents-*.json` mkstemp temps), and
  `dashboard/agents/test_generate.py` exists — 941 lines, part of a suite that
  runs 273 tests green. Nine files tracked: the generator, its tests,
  `agents.json` config, the systemd service + timer, `agents.html`, the index
  link, the README, and `.gitignore`. Verified from `ci-ops-self-hosted` on
  2026-09-13; this repo held the note but never got a write-back, which is why
  it read as pending. — 2026-09-13
- Context doc for the above (also local-only):
  `docs/plans/2026-09-11-ai-harness-landscape-and-fleet-strategy.md` — harness
  definition, field survey, rated options, and the three quick wins applied
  2026-09-12 (global worktree hook, cepa 1.23.4 → 1.25.2, `/cepa:sweep`
  scheduled Sundays 05:00 from `~/.cache/cepa-cron`). — 2026-09-13
- **Unexercised:** the sweep's `claude -p` invocation has never run. Every guard
  around it was verified, the call itself was not. First real run Sunday
  2026-09-13 05:00; log at `~/.cache/cepa-sweep-logs/`. The agent dashboard
  flips that loop `never ran` → `alive` when it fires. — 2026-09-13
- **`plugins/cepa/scripts/capture.py` is NOT this repo's to commit — leave it.**
  783 lines, untracked, written 2026-09-12 18:34. Author identified by the
  operator 2026-09-13: **Claude Desktop**, Cowork project *"Evan's AI System
  Build"*, led from session `cse_01XbcucUbNMCWffiUwzvEqaH`. It will be committed
  from there, on the operator's schedule.
  Do not commit, move, delete, or "tidy" it from a CLI session.
  Why a CLI session cannot find its author: Claude Desktop writes no transcript
  into `~/.claude/projects/`, so a search there returns zero write ops on the
  path and looks like an orphan. The corroborating tell is in its own docstring
  — it walks `C:\Users\evan\ClaudeWork\<Client>\`, a **Windows** path, while
  everything in this WSL tree is `/home/evan`. It self-describes as the third of
  three brain pipes: `/cepa:compound` (engineering knowledge in, built),
  `capture.py` (business knowledge in, this file), `bundle.py` (curated
  knowledge out, not built). — 2026-09-13

## 2026-09-13 — handoff branch-disposition + sweep merged-branch inventory

Work from the session that built `/cepa:handoff`'s readiness verdict and
`/cepa:sweep`'s merged-branch inventory. All six PRs merged to `main`; recorded
here because nothing else in this shard covers them.

- [x] **`/cepa:handoff` — readiness verdict + branch disposition.** PR #50,
  squash `a7e91981`. Emits `GO`/`WAIT`/`GO WITH CARE` (advice, never a gate —
  computed once, rendered twice, no `if WAIT` anywhere) and resolves which
  branch the next session belongs on. Review found 4 P1s on the destructive
  path, all reproduced in scratch repos before being accepted and re-verified
  after fixing: a merged PR is not a merged branch (post-merge commits deleted
  silently); a failed `checkout -b` did not stop the following `-D`; multiple
  PRs per head with no tie-break; `[]`+exit 0 uncaught by an `Unknown` defined
  as call failure. Findings: `todos/review-2026-09-01-071500.md`. — 2026-09-13
- [x] **`/cepa:sweep` source 5 — merged local branches, report-only.** PR #51,
  squash `d6738100`. Closes the D2 deferral filed 2026-09-01. One bulk
  `gh pr list` instead of one call per branch. Review found the cited safety
  conditions were not *evaluable* by the query underneath — `--state merged`
  made condition 1 vacuous, and `git branch` parsing dropped the `+`
  worktree marker. Findings: `todos/review-2026-09-06-090000.md`. — 2026-09-13
- [x] **Four field-driven corrections**, each from a real dogfood rather than
  review: PR #52 `f3910944` (window 200 → 500), #53 `dbed1b64` (worktree-held
  evaluated first; `no merged PR` split into `closed PR only` /
  `no PR ever opened`), #54 `a393f9fc` (`multiple merged PRs on one head` named;
  fixtures for the three classes no repo can produce), #55 `a7e1a142`
  (window → 1000). cepa 1.21.x → **1.25.2**. — 2026-09-13
- [x] **`scripts/check-sweep-branch-classes.sh`** — 8 fixtures in
  `residual-integrity.yml` with `if: always()`, covering
  `multiple merged PRs on one head`, `non-trunk base`'s negative path, and
  `MERGED`+`OPEN` on one head. Verified across 8 repos that **zero** heads carry
  both MERGED and OPEN, so that class is unreachable by any real run and would
  otherwise be untested. — 2026-09-13
- [x] **Two of this session's own shipped claims were falsified by testing, not
  review** — worth keeping as the pattern: (1) the first fix for the
  post-merge-commits P1 used `rev-list` ancestry, which is false for *every*
  squash-merged branch (a squash commit has no parent link to the branch), so it
  would have classified all merged branches `Unknown` and deleted nothing ever;
  replaced with `headRefOid` equality. (2) #53 claimed the 500-PR window was
  "~18 months out" — `artist-360` was already at **511 PRs** and saturating it
  that day. Both defaults had been sized against whatever repo was measured most
  recently. — 2026-09-13

### Write-back — actions taken OUTSIDE this repo

- [x] **`delete_branch_on_merge` enabled on 18 repos**, 2026-09-12. It was
  `false` on all 54 repos in the account; scoped to non-fork repos with real
  branch accumulation. Re-verified 18/18 today by independent read, not by
  trusting the PATCH echo. This is the root-cause fix for local-branch
  accumulation, and it covers **only the remote half** — the local branch and
  its stale remote-tracking ref both survive a merge. Confirmed end-to-end on
  PR #55's own merge: remote head 404'd, `git fetch --prune` pruned a ref for
  the first time (it was previously a no-op because nothing ever deleted a
  remote head), local branch survived. — 2026-09-13
- [x] **dpc-insider-www cleanup ran** — `trunk: main` is present in that repo's
  `cepa.local.md` on its default branch, and its local branches went **152 → 42**
  (the inventory's 110 listed entries deleted). Verified read-only via
  `git -C` and the GitHub API from this repo. — 2026-09-13
- [ ] **dpc-insider-www Part 3(b) still open** — the publishing pipeline should
  delete the *local* branch at merge time, with the same tip-vs-`headRefOid`
  check the cleanup used. `delete_branch_on_merge` does not cover this. Remote
  branches there are still ~230, and 183 of its merged PRs are `draft/*` at
  roughly one per day, so the local backlog regrows to ~110 within a year
  without it. Prompt (machine-local, this box only):
  `/tmp/claude-1000/-home-evan-webapps-compound-engineering/b200a003-c181-4d90-867c-35b4b210dbe3/scratchpad/dpc-insider-www-cleanup-prompt.md`
- [ ] **`artist-360` never inventoried** — 511 PRs, 182 local branches, the
  largest in the portfolio and the repo that falsified the 500 window. Now
  within the 1000 window, so a run would report `complete`. — 2026-09-13
- [ ] **The `MERGED`+`OPEN` path has no real-world coverage** and cannot get any
  from this portfolio (0 occurrences across 8 repos). It stays covered by
  `scripts/check-sweep-branch-classes.sh` alone. If a repo ever produces one,
  that run is worth more than the fixture. — 2026-09-13

## 2026-09-13 — session handoff (residual-integrity and node20 closeout)

- Handoff written local-only (`docs/` is gitignored in this repo, and the repo
  is PUBLIC — so the file is deliberately not committed):
  `/home/evan/webapps/compound-engineering/docs/handoff/2026-09-13-residual-integrity-and-node20-closeout.md`
  — subject: the 2026-09-01 work (PR #49 `check-residual-integrity`, merged
  `e5bba4fb`; node20 sweep closed `5f00172`) plus this session's close-out.
  Treat the pointer as machine-local: a fresh checkout cannot follow it.
  Supersedes `docs/handoff/2026-09-12-brain-client-idkey-defect-class.md`,
  stamped in place.

  **Deliberately did NOT supersede
  `docs/handoff/2026-09-13-agent-fleet-dashboard-landing.md`**, which sorts
  later. That file was a cross-repo ACTION PROMPT, not a session record —
  stamping it SUPERSEDED would have marked live, pending work as history,
  which is the exact loss a handoff exists to prevent.

  **Closed later the same day:** the landing ran (PR #20, squash
  `9824ddb0`), so that file is now stamped **DONE — executed**, not
  SUPERSEDED. The distinction is the point and is worth keeping: a prompt that
  was *executed* is closed by its outcome, while a prompt that was *replaced*
  is closed by a successor. Stamping DONE says "do not re-run this"; stamping
  SUPERSEDED would have said "read the newer one instead", which is false —
  there is no newer one, the work simply landed.

  Unsettled items carried in the handoff: how `§7` is excluded when
  `check-model-pins.sh` leg 4 widens to bare `§N` (blocks the only open P1);
  which of three options closes the leg-4 P3 (control `real` moves in the same
  commit). — 2026-09-13

  **The first of those is CLOSED 2026-09-14** on `fix/cite-leg4-bare-anchors`.
  The question was malformed: `§7` is CHECKED, not excluded. Canonical
  reasoning lives in
  `memory/tasks.d/2026-07-30-fix-cite-trunk-ladder-once.md`, cited not
  restated. Recorded here because this shard is the third independent copy of
  the claim — a reader who opened it instead of the canonical shard would have
  seen a closed P1 described as open and blocking. Found by
  `previous-comments-reviewer` on the PR-60 review.
